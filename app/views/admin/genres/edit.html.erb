<h4 class="m-5 w-25 bg-light text-center">ジャンル編集</h2>
<%= form_with model: @genre, url: admin_genre_path do |f| %>
  <table>
    <%= f.label 'ジャンル名', class: "mr-5 ml-3" %>
    <%= f.text_field :name, class: "mr-5" %>
    <%= f.submit '変更を保存', class: 'btn btn-success' %>
  </table>
<% end %>